# 《鲜花与牛粪》游戏 — 开发文档 / 验收报告

> 版本：v3.1（与当前代码实现对齐，2026-08-27）
> 技术栈：客户端 **Android（纯 Java / 代码动态布局）** + 后端 **Supabase（Auth / PostgreSQL / REST）**
> 本文档同时作为**开发参考**与**验收报告**使用。带 `（验收）` 的章节给出可在真机上执行的验收步骤。

---

## 1. 项目概述

- **项目名称**：鲜花与牛粪
- **玩法**：双人实时回合制棋类（Nim 取子变体）。轮流拾取鲜花，最终被迫拿走场上最后一枚"牛粪"者判负。
- **三种模式**：**人机对战（PvE）** / **人人游戏大厅（PvP）** / **私密房间（好友约局）**。三种模式共用同一套规则引擎与棋盘组件，均进入同一个对局页 `LocalGameActivity`。
- **积分制**：每局胜负写入数据库，按**最终得分**排名。
- **工程形态**：客户端全部界面用 Java 代码动态拼装（`new View(...)`），**无 XML 布局、无 Compose、无第三方网络 SDK**（仅官方 `HttpURLConnection` + 自研 `SupabaseClient`）。

## 2. 技术选型（实际）

| 分层 | 选型 | 说明 |
| --- | --- | --- |
| 客户端 | Android Studio + **纯 Java（动态布局）** | 所有界面用代码构建 |
| 架构 | 轻量 MVC（Activity + 自定义 View） | Activity 持状态，自定义 View 封装棋盘/桌子/地板 |
| 本地存储 | `SharedPreferences`（key=`CowDungPrefs`） | 存登录态、昵称、性别、密码（明文，仅本地） |
| 后端 | **Supabase** | 云托管：Auth / Postgres / REST |
| 后端访问 | Supabase **REST（HttpURLConnection）** | 自研 `SupabaseClient`，**无官方 SDK、无 WebSocket** |
| 实时性 | **轮询（polling）** | 各 Activity 用 `Handler.postDelayed` 周期拉取 REST 接口，无 Realtime/Presence/Broadcast |
| 认证 | Supabase Auth **邮箱/密码** | 注册时邮箱由昵称派生（`nickToEmail`），昵称即登录名 |
| 敏感词过滤 | `com.github.houbb:sensitive-word:0.29.5` | 注册昵称 + 聊天发送双重过滤 |
| 数据库 | Supabase PostgreSQL + **RLS** | 用户 / 桌 / 房间 / 观战 / 聊天 / 对局 / 排名 |

> ⚠️ 与旧版文档区别：旧文档声称"匿名登录 + Realtime WebSocket + okhttp3/gson 依赖"，**均与实际代码不符**。当前实现为账号密码登录 + REST 轮询，依赖里没有 okhttp3/gson。

## 3. 三种游戏模式总览

| 模式 | 入口 | 对手 | 是否需要网络 | 计分 |
| --- | --- | --- | --- | --- |
| **PvE 人机** | 首页「人机游戏大厅」 | 电脑 AI（Nim 最优） | 需要（落子/状态上报/计分走服务端） | 胜 **+1**，负 **0**（不扣分、总场次 +1） |
| **PvP 人人大厅** | 首页「人人游戏大厅」 | 随机路人 | 必须在线 | 胜 **+5**，负 **−1** |
| **私密房间** | 首页「私密房间」→ 建房/输 4 位房号 | 指定好友 | 必须在线 | 胜 **+10**，负 **−2** |

- 三种模式均进入 `LocalGameActivity`，由 `Intent` 的 `source` 字段区分：`pve` / `pvp` / `room`。
- 私密房间在 `LocalGameActivity` 内 `isRoom=true`，并以 4 位 `room_code` 同时作为 `tableNo` 使用（见 §9、§19）。

## 4. 核心游戏规则（基准规则，勿改）

1. 双方轮流操作，每回合**从某一排拿走 1 枚或任意多枚鲜花**。
2. 一次只操作**一排**，不支持跨排。
3. **牛粪不能被拿起或选中**，牛粪排（第 1 排）固定保留 1 枚。
4. 鲜花（第 2~6 排）被全部拿完时，**取空最后一朵鲜花的玩家获胜**；轮到的一方被迫拿走牛粪即判负。
5. 无平局、无和棋。

**先手规则**
- PvE：玩家先手；按局数交替（第二局电脑先手）。
- PvP / 私密：谁先入座谁先手（开局由 `pvp_ready` / `room_ready` 服务端原子判定，双方均点「准备好了」后进入对局并确定先手）。

**回合倒计时**（服务端权威 `turnDeadlineAt`）
- PvP / 私密：每人 45 秒，超时判负。
- PvE：玩家 180 秒、电脑 5 秒（宽松，不构成主要压力）。

## 5. 棋盘布局与交互

| 排（索引） | 初始棋子 | 内容 | 可选 |
| --- | --- | --- | --- |
| 第 1 排（i=0） | 1 | 💩 牛粪 | ❌ |
| 第 2~6 排（i=1~5） | 2~6 | 🌹 | ✅ |

- 数据结构：`remainingFlowers = int[]{1,2,3,4,5,6}`；选中态 `selectedFlowers[行][朵]`。
- 选中视觉：🌹 透明度降为 50%；再点取消。
- 交互：点 🌹 选中/取消；按钮实时显示"鲜花拿来 (N 朵)"；点「确认选择」执行 `remainingFlowers[selectedRow] -= count`；点 💩 提示"不能拿牛粪"。
- 对局页布局（自上而下，代码 `LinearLayout`+`weight`）：对战信息条 8% / 棋盘区 42% / 操作按钮区 10% / 日志+聊天区 40%。

## 6. 胜负判定与电脑 AI

**判定（客户端为主、服务端复核）**
- 计分对局：每回合落子后 `sum = 第2~6排剩余`；`sum==0` → 本回合取花者为胜，调 `finish_game` 写库 + 计分，广播/刷新排行，重置棋盘。
- PvE：同样判定胜负，调 `pve_finish`（胜 +1），**不写 `games` 表**（仅更新 `profiles` 的 score/wins/total_games）。

**ComputerAI（Nim 最优策略，独立模块，双用）**
- `nimSum = r1^r2^r3^r4^r5`；`nimSum!=0` → 取使归零的走法；`==0` → 必败局随机走。
- 接口：`ComputerAI.getNextMove(int[])`、`ComputerAI.getHint(int[])`，返回 `{row,count}` 或 `null`。
- 双人对局中"提示走法"= 长按「确认选择」3 秒，高亮推荐花（只读，不影响对局）。

## 7. 模式一：人机对战（PvE）

- 入口：`PvELobbyActivity`（大厅展示若干桌 `pve_tables`，点桌弹【坐下玩游戏 / 当观众】）。
- 坐下 → `pve_sit` → 进入 `LocalGameActivity(source=pve)` → `pve_start` 开始 → 玩家/电脑轮流 → 判胜 → `pve_finish` 计分（+1）→ 可再来一局。
- 退出：`pve_leave`；观战：`pve_watch` / `pve_unwatch`；状态上报：`pve_report_state`；心跳：`pve_heartbeat`；认输：`pve_forfeit`。
- **PvE 不计负分、只加胜场分**。

## 8. 模式二：人人游戏大厅（PvP）

- 入口：`PvPLobbyActivity`（展示 `pvp_tables`，点桌弹【坐下 / 当观众】）。
- 入座：`SeatManager.pvpSitAsPlayer(tid)` → 调 `pvp_sit_a` / `pvp_sit_b`（按空位）；双方就位后均点「准备好了」→ `pvp_ready` 原子判定先手 → 开局。
- 对局：`LocalGameActivity(source=pvp)`，状态上报 `pvp_report_state`，心跳 `pvp_heartbeat`，结束 `pvp_end`。
- 计分：结束调 `finish_game(room_type='lobby', winner, loser)` → 胜 +5 / 负 −1。
- 观战：`pvp_watch` / `pvp_unwatch`，名单存 `pvp_watchers`。

## 9. 模式三：私密房间（好友约局）

- 入口：`PrivateRoomActivity`（建房得 4 位 `room_code` / 输房号进房）。
- 房号即准入凭证（不设密码），不在大厅公开。进房后点桌【坐下 / 当观众】。
- 对局：`LocalGameActivity(source=room, room_code=XXXX)`，内部 `isRoom=true` 且 `tableNo=room_code`，状态上报 `room_report_state`，准备 `room_ready`，结束 `room_end`，观战 `room_watch`。
- 计分：结束调 `finish_game(room_type='private', winner, loser)` → 胜 +10 / 负 −2。
- 说明：当前代码未实现"房主踢人/禁言"与"房主权限转移"等旧文档描述的房管功能（见 §22 已知限制）。

## 10. 积分与排名系统（实际实现）

### 10.1 计分规则（落库于 `supabase/ranking_scores.sql` 的 `finish_game` / `pve_finish`）

| 模式 | 胜者 | 败者 |
| --- | --- | --- |
| 人人大厅（lobby） | **+5** | **−1** |
| 私密房间（private） | **+10** | **−2** |
| 人机（PvE） | **+1** | **0**（仅总场次 +1，分数不变） |

- 写入字段：`profiles.score` / `wins` / `losses` / `total_games`，并在 `finish_game` 内**同一事务原子完成**；同时插入 `games` 记录。
- **防重入（每局只结算一次）**：`finish_game` 在 `pvp_tables` / `private_rooms` 的 `game_state` 里置 `scored=true` 标记；已标记则跳过，重开对局由开局 RPC 重置 `game_state`（清除 `scored`）。PvE 因不写 `games` 且以 `scored` 标记幂等，重赛不会重算。

### 10.2 排名计算（实际）

- 排序键（与客户端展示一致）：`score DESC, wins DESC, score_reached_at ASC`（同分先到者靠前）。
- `get_ranking(limit_n)`：返回 `id, nickname, gender, score, wins, losses, total_games`（已过滤 `nickname IS NOT NULL`），按上述三键排序，取前 `limit_n`。
- `get_user_rank(in_user_id)`：用同样的比较键算出单人真实名次 `rank` 及 `score/wins/losses/total_games`。
- **军衔映射**：`ProfilePopup.levelName(score) = LEVELS[score/200]`，`LEVELS = 新兵/士兵/班长/排长/连长/营长/团长/旅长/司令/军长/元帅`（每 200 分一档）。

### 10.3 展示位置

- **首页「排行榜」按钮**（`MenuActivity.btnRank`）→ `RankingBoard.show()` 弹出**全员总榜面板**：
  - 顶部：标题"排行榜" + ✕ 关闭；次行"我的排名：第 N 名 · 积分 X · 军衔 Y"（无排名显示"暂无"）。
  - 每行折叠态 4 列：`序号 | 昵称（带性别圆点：男蓝/女粉/其它灰）| 军衔 | 总积分`。
  - 点某行展开明细：`赢 X · 输 Y · 场次 Z`（单开，再点收起）。
  - 本人那行浅蓝高亮；点遮罩外或 ✕ 关闭；数据 `getRanking(1000)` 覆盖所有人员。
  - **加载中转圈**：拉取 `getRanking(1000)` 期间显示进度圈，数据返回后再渲染列表，避免空面板闪烁。
- **对局页点昵称**（`LocalGameActivity.openProfile`）→ `ProfilePopup.show(...)` 半屏圆角卡：昵称/军衔/排名/积分/胜/负，**5 秒自动隐藏**，点任意处关闭。

## 11. 用户身份体系（实际：账号密码登录）

- **注册**（`AuthActivity.doRegister`）：昵称（1~4 字，敏感词过滤）+ 性别（男/女）+ 密码（≥6 位）→ `check_nickname` 查重 → `signUp`（邮箱=`nickToEmail(nick)`，密码同输入）→ `create_profile(nick, gender)` 建档。重名提示更换。
- **登录**（`AuthActivity.doLogin`）：昵称 + 密码 → `signIn`（用派生邮箱）。成功后本地 `CowDungPrefs` 存登录态/昵称/性别/密码。
- **会话恢复**：`SupabaseClient` 持久化 access/refresh token；启动若 `LoggedIn=true` 且 `hasSession()` 有效则直进 `MenuActivity`。
- **未登录不可进入游戏**：`AuthActivity` 是 LAUNCHER，`MenuActivity` 及其后页面必须已登录。
- 昵称/性别决定头像（`man.png` / `women.png`）。

## 12. 同步与心跳（实际：轮询）

- 各对局/大厅页用 `Handler.postDelayed` 周期（常见 0.5~2s）调用 `SupabaseClient` 的 `fetch*Tables` / `fetchRoom` / `fetchWatcherNicknames` / `fetchChatAfter` 拉取最新状态并刷新 UI。
- **权威来源 = 服务端 DB**：客户端收到轮询结果即按返回渲染，不依赖本地缓存作为真相。
- 心跳：`pve_heartbeat` / `pvp_heartbeat` / 私密房间心跳，周期上报在线；离线由服务端逻辑（或重新进入时状态）处理。
- 退出：对局页「退出」按钮调 `pve_leave` / `pvp_leave` / `room` 离场 RPC 写库，大厅立即刷新空座。

## 13. 聊天系统（已实现，落库）

- 表 `chat_messages(id, table_id, sender_id, sender_name, message, created_at)`，**落库可回看**：`fetchChatHistory` / `fetchChatAfter` 拉取历史。
- 发送：`sendChat(tableId, senderId, senderName, message)` → 写 `chat_messages`；发送前经 `BadWordFilter.filter` 替换敏感词为 `*`，命中额外提示。
- 限流与长度由调用处控制；仅纯文字。

## 14. Supabase 数据模型（实际表）

> 建库脚本见 `supabase/*.sql`。聊天 `chat_messages`、对局 `games`（由 `finish_game` 创建）均落库。

### profiles
`id` uuid PK（auth.users）· `nickname` text（唯一）· `gender` text（male/female）· `score` int default 0 · `wins` int default 0 · `losses` int default 0 · `total_games` int default 0 · `score_reached_at` timestamptz（排名同分先后依据）· `last_seen_at` / `created_at` timestamptz

### pve_tables
`id` · `num`（桌号）· `player_id` uuid（可空）· `status`（idle/playing/finished）· `game_state` jsonb · `watcher_count` int · 观战存 `pve_watchers(table_id, user_id)`

### pvp_tables
`id` · `player_a_id` / `player_b_id` uuid（可空）· `status` · `game_state` jsonb · `watcher_count` int · 观战存 `pvp_watchers(table_id, user_id)`

### private_rooms
`room_code` char(4) PK · `host_id` uuid · `player_a_id` / `player_b_id` uuid · `status` · `game_state` jsonb（观战名单由 `pvp_watchers` 同类机制 / `game_state` 中维护，见 §22）

### chat_messages
`id` bigserial · `table_id` text（桌号或房号）· `sender_id` uuid · `sender_name` text · `message` text · `created_at` timestamptz

### games（结算写入）
`id` uuid PK · `room_type`（lobby/private，PvE 不写）· `table_id` / `room_code` · `player_a_id` / `player_b_id` · `winner_id` / `loser_id` · `score_delta` jsonb · `moves` jsonb · `finished_at` timestamptz

## 15. 后端 RPC / REST 接口（实际）

> 全部经 `SupabaseClient.rpc(name, args)`（POST `/rest/v1/rpc/<name>`）或 REST 直读（GET `/rest/v1/<table>`）。参数名必须与函数参数严格一致（曾因客户端用 `user_id` 而函数用 `in_user_id` 导致 404，已修正为客户端传 `in_*` 前缀）。

**认证**
- `POST /auth/v1/signup`（注册，邮箱派生）· `POST /auth/v1/token?grant_type=password`（登录）· `refresh_token` · `POST /auth/v1/logout`
- `check_nickname(n)` · `create_profile(nick, g)`

**PvE**：`pve_sit` · `pve_leave` · `pve_watch` · `pve_unwatch` · `pve_start` · `pve_end` · `pve_heartbeat` · `pve_forfeit` · `pve_report_state(tid, state)`

**PvP**：`pvp_sit_a` · `pvp_sit_b` · `pvp_leave` · `pvp_watch` · `pvp_unwatch` · `pvp_ready` · `pvp_end` · `pvp_heartbeat` · `pvp_report_state(tid, state)`

**私密房间**：`room_report_state(code, state)` · `room_ready(code)` · `room_end(code)` · `room_watch(code)`（建房/进房/离房 RPC 见 `supabase/private_rooms.sql`）

**积分/排名**：`finish_game(in_table_id, in_room_code, in_room_type, in_winner_id, in_loser_id, in_moves)` · `pve_finish(in_player_id, in_won)` · `get_ranking(limit_n)` · `get_user_rank(in_user_id)`

**REST 直读表**：`profiles` · `pve_tables` · `pvp_tables` · `private_rooms` · `pve_watchers` · `pvp_watchers` · `chat_messages`

> 所有写操作（落子/结算/入座）均应在服务端 RPC 内用 `auth.uid()` 二次校验，客户端不可伪造（RLS 见 §21）。

## 16. Android 工程结构（实际文件）

```
app/src/main/java/com/example/cowdunggame/
├── GlobalApplication.java     // Application（初始化）
├── AuthActivity.java          // 登录/注册（昵称+性别+密码，敏感词过滤）
├── MenuActivity.java          // 登录后主菜单（排行榜/人机大厅/人人大厅/私密房间/注销）+ 装饰桌景
├── PvELobbyActivity.java      // 人机大厅（pve_tables 列表、坐下/观战）
├── PvPLobbyActivity.java      // 人人大厅（pvp_tables 列表、坐下/观战）
├── PrivateRoomActivity.java    // 私密房间（建房/进房、单桌场景）
├── LocalGameActivity.java     // 唯一对局页（PvE/PvP/私密 三模式合一；棋盘+操作+日志+观众+聊天+资料卡）
├── SeatManager.java           // 入座/观战/心跳/准备 的 RPC 封装与回调
├── SupabaseClient.java        // 自研 REST 客户端（认证/RPC/会话/轮询拉取/聊天/排名）
├── ComputerAI.java            // Nim 最优策略（人机落子 + 双人提示，双用）
├── BadWordFilter.java         // 敏感词过滤（封装 houbb sensitive-word）
├── ProfilePopup.java          // 点昵称资料卡（半屏圆角，5 秒自动隐藏；支持锚定位置）
├── RankingBoard.java          // 全员总榜面板（序号/昵称/性别/军衔/积分，点击展开胜负场次）
├── TableDishView.java         // 大厅桌组件（桌面+四边头像/座位，尺寸按屏比计算）
├── GameTableView.java         // 对局桌卡（左/右玩家 + 上/下观众位）
├── FloorView.java             // 地板砖背景
├── BgmManager.java            // 全应用背景音乐（MediaPlayer 循环，音量 0.4，受前台+音乐开关控制）
├── SoundSettingsDialog.java   // 声音设置弹窗（音效/音乐双开关，圆角）
└── AppDialog.java             // 通用确认/提示对话框
```
> 无 `MainActivity` / `RealtimeClient` / `SupabaseIdentity` / `UpdateManager` / `CrashHandler` 等旧文档所列文件——那些是旧版设计，当前代码未实现（注：曾有分析基于一份**过时的克隆**，误报"纯单机 PvE、无 Supabase、存在 MainActivity/UpdateManager"，与实际仓库不符，已在此更正）。

### 16.1 依赖（app/build.gradle 实际）
```
implementation 'androidx.appcompat:appcompat:1.6.1'
implementation 'com.google.android.material:material:1.12.0'
implementation 'com.github.houbb:sensitive-word:0.29.5'   // 敏感词过滤
// 注意：无 okhttp3、无 gson、无 Supabase 官方 SDK
```
- `compileSdkVersion 36` · `minSdkVersion 28` · `targetSdkVersion 36` · `namespace=com.example.cowdunggame`
- 仅 `release` 类型，未启用混淆（`minifyEnabled false`）。

## 17. 核心页面规范（实际）

### 17.1 登录/注册（AuthActivity）
 整页**背景图 + 中央小白花**（`white_flower`）装饰，白色圆角卡片叠于其上：标题、状态提示、昵称输入框（限 4 字以内）、性别（男/女，注册必选）、密码（≥6 位）、注册/登录按钮、模式切换。注册经敏感词过滤 + 查重；登录用昵称+密码。
 - **密码眼睛**：输入框右侧眼睛图标（`ic_eye_open` / `ic_eye_closed`）切换明文/密文；密文统一渲染为 `*`（个数=密码长度），用状态变量控制图标与显隐，不残留明文。

### 17.2 主菜单（MenuActivity）
- 背景 `FloorView` 浅蓝地板；中央一张装饰性大桌卡 `GameTableView`（左男右女、上下观众满员，纯展示）。
- 标题"鲜花与牛粪"（金色描边）。
  - **「排行榜」按钮**（标题下方，无边框透明）→ `RankingBoard` 全员总榜。
  - **右上角「声音」按钮**（`btnSound`，无边框透明）→ `SoundSettingsDialog` 双开关声音设置（见 §25）。
  - **底部 2×2 按钮网格**（屏比权重布局）：人机游戏大厅 / 人人游戏大厅 / 私密房间 / 注销。
- 无独立"实时大厅列表页"——各模式的大厅是独立 Activity。

### 17.3 人机大厅（PvELobbyActivity）
展示若干 `pve_tables`（编号桌），点桌弹【坐下玩游戏 / 当观众 / 取消】；坐下/观战进入 `LocalGameActivity(source=pve)`。

### 17.4 人人大厅（PvPLobbyActivity）
展示 `pvp_tables`，点桌弹【坐下 / 当观众】；入座经 `SeatManager` 分配 A/B 座，双方就绪后开局。

### 17.5 私密房间（PrivateRoomActivity）
建房得 4 位房号（顶栏显示、可复制）或输房号进房；单桌场景，点桌坐下/观战 → `LocalGameActivity(source=room, room_code)`。

### 17.6 对局页（LocalGameActivity）
- 顶部对战信息条（双方昵称/头像、倒计时、当前回合指示）。
- 棋盘区（`GameTableView` + `FloorView` 背景 + 花朵 `ImageView`）。
- 操作按钮区：确认选择/鲜花拿来、退出棋局等。
- 日志区：标题「观众列表」展示 `fetchWatcherNicknames` 返回的昵称；「游戏日志:」副标题下显示落子与胜负。
- 底部聊天栏：昵称按钮(15%) + 输入框(70%) + 发送(15%)，发送经敏感词过滤。
- 点昵称 → `ProfilePopup` 资料卡。

## 18. 游戏状态同步（实际）

- 服务端 `game_state` 为权威 JSON（含 `rows` 剩余数、`turnUserId`、`turnDeadlineAt`、`phase`、`winnerId`、`loserId` 等，结构见旧 §19，仍适用）。
- 落子方：本地乐观操作 → `pvp_report_state` / `room_report_state` / `pve_report_state` 写 DB → 轮询拉取覆盖校准。
- 对方/观战：仅轮询拉取 DB 渲染。
- 断线重连：重新进入桌/房后先拉取一次 `game_state` 恢复，再继续轮询。

## 19. 关键流程时序（实际）

**登录**：启动 → `AuthActivity` → 已登录且会话有效则直进菜单；否则注册/登录 → `create_profile`/`signIn` → 存 `CowDungPrefs` → 进 `MenuActivity`。

**PvE**：菜单→人机大厅→点桌坐下(`pve_sit`)→`pve_start`→玩家/电脑轮流(`pve_report_state`)→判胜→`pve_finish`(+1)→再来一局(`pve_end`/`pve_leave`)。

**PvP**：菜单→人人大厅→点桌坐下(`pvp_sit_a/b`)→双方「准备好了」(`pvp_ready`)→开局→轮流(`pvp_report_state`)→`finish_game`(胜+5/负−1)→排行刷新。

**私密**：菜单→私密房间→建房/进房→点桌坐下→双方准备(`room_ready`)→开局→`finish_game`(胜+10/负−2)→排行刷新。

**同步**：各页 `Handler.postDelayed` 周期拉取 REST → 以服务端数据为准刷新 UI。

## 20. 安全与权限（RLS）

- `profiles`：`select` 公开（排行需要）；`update/insert` 仅自己。
- `pve_tables` / `pvp_tables` / `private_rooms`：`select/insert` 登录用户开放；落子/结算走 RPC 校验（座位持有者才能动棋）。
- `chat_messages` / 观战表：登录用户可读写各自记录。
- 房主操作、落子校验均应在服务端 RPC 内用 `auth.uid()` 二次校验。
- 客户端使用 Supabase **publishable key**（`sb_publishable_*`）+ 用户 JWT，配 RLS 后可安全公开。

## 21. 验收标准（验收报告）

> 下列条目为**按当前代码应可验收**的功能；建议在真机/模拟器逐项执行。标注 `（待真机验证）` 的表示代码已实现但需端到端联调确认。

1. **注册/登录**：昵称 1~4 字、性别必选、密码≥6 位；重名提示更换；含敏感词昵称被拒；登录后本地持久化，重启免登录。
2. **未登录不可游戏**：直接启动停在 `AuthActivity`，无法进入大厅/对局。
3. **三种模式入口齐全**，共用同一规则引擎。
4. **PvE**：符合第 4 章规则；电脑 AI 满足 Nim 最优（必败局随机）；**胜 +1、负 0、总场次 +1、不写 `games`**。
5. **PvP 计分**：结束调 `finish_game(room_type=lobby)`，胜方 `score+5`、负方 `score−1`；`wins/losses/total_games` 正确。
6. **私密计分**：结束调 `finish_game(room_type=private)`，胜方 `score+10`、负方 `score−2`。
7. **防重入**：同一局重复结算只生效一次（`scored` 标记），重开后可再次结算。
8. **排名正确**：`get_ranking` 按 `score desc, wins desc, score_reached_at asc`；`RankingBoard` 显示序号/昵称/性别点/军衔/积分，点行展开胜负场次，本人行高亮，顶部"我的排名"正确。
9. **资料卡**：对局页点昵称弹 `ProfilePopup`，显示军衔/排名/积分/胜/负，5 秒自动隐藏。
10. **回合计时**：轮到谁面前 45 秒倒计时（PvP/私密），超时判负（服务端 `turnDeadlineAt` 权威）。（待真机验证）
11. **观战**：`pve_watch`/`pvp_watch`/`room_watch` 后进入观战模式，观众列表显示昵称，落子仅旁观。
12. **聊天**：`chat_messages` 落库可回看；发送敏感词被替换为 `*` 并提示。（待真机验证）
13. **同步以拉取为准**：轮询拉取 DB 渲染，不依赖本地缓存。
14. **退出写库**：对局页退出按钮调离场 RPC，大厅/房间立即刷新空座。

## 22. 已知限制 / 与旧设计差异（重要）

- **无实时推送**：当前为 `Handler` 轮询，非 WebSocket/Realtime；高频对局流畅度依赖轮询间隔（默认 0.5~2s），不是毫秒级实时。
- **私密房间房管功能缺失**：旧文档描述的"房主踢人/禁言/权限转移"在现有 `SupabaseClient` 与 `PrivateRoomActivity` 中**未实现**（仅 `room_watch`/`room_ready`/`room_end`/`room_report_state` 等基础 RPC）。
- **PvP 入座/准备/先手**依赖 `SeatManager` + `pvp_ready` 原子判定，需服务端 `pvp_tables` 相应 RPC 与列存在且正确（与 `supabase/pvp_tables.sql` 对齐）。
- **昵称即登录名**：注册邮箱由昵称派生（`nickToEmail`），同一昵称对应同一账号；测试环境若 Supabase 要求邮箱验证，未验证账号登录可能受限（旧文档已记录 `confirm_required` 提示）。
- **旧文档架构描述（匿名登录、Realtime、okhttp3/gson、MainActivity、RealtimeClient 等）与当前代码不符，已在本版更正。**
- **构建产物仍被 Git 跟踪**：`app/release/app-release.apk`、`app/release/baselineProfiles/*.dm`、`app/release/output-metadata.json` 目前在历史提交中已存在、仍被跟踪（每次提交需手动 `git reset` 排除）。建议加 `.gitignore` 并从历史中清理，避免仓库膨胀。

## 23. 构建与运行

- 环境：Android Studio，SDK 36，Java 11；Gradle 8.13（`gradlew` 包装）。
- 依赖 Supabase 项目（URL + publishable key 写在 `SupabaseClient` 常量 `PROJECT_URL` / `ANON_KEY`）。
- 数据库：在 Supabase SQL Editor 依次执行 `supabase/schema.sql` 及相关 `fix_*.sql`、`ranking_scores.sql`、`pvp_tables.sql`、`private_rooms.sql`、`pve_tables.sql`、`chat_messages.sql`（执行后建议 `NOTIFY pgrst,'reload schema'`）。
- 构建：`gradlew assembleDebug`（或 `compileDebugJavaWithJavac`）。
- 安装：生成 APK 装到设备/模拟器，打开即 `AuthActivity`。

## 24. 开发里程碑（★= 代码已落地）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M0★ | 工程骨架 + 纯 Java 动态布局 + 自定义棋盘/桌子/地板 View | 已落地 |
| M1★ | 账号密码登录/注册 + 敏感词过滤（昵称+聊天） | 已落地 |
| M2★ | 核心规则引擎 + ComputerAI + PvE（大厅/对局/人机） | 已落地 |
| M3★ | 人人大厅（建桌/入座/准备/对局/观战） + 私密房间（建房/进房） | 已落地 |
| M4★ | 积分/排名体系（finish_game/pve_finish/get_ranking/get_user_rank）+ RankingBoard + 资料卡 | 已落地 |
| M5 | 房主踢人/禁言、权限转移 | 未实现 |
| M6★ | 全应用 BGM（按节拍对齐无缝循环）+ 双开关声音设置 + 登录背景/小白花 + 密码眼睛 + 排行榜加载动画 | 已落地（v3.1） |
| M7 | 真机联调、轮询节奏打磨、压测、发布 | 进行中 |

---

## 25. 背景音乐与声音设置（实际新增，v3.1）

> 以下功能在 v3.0 之后落地（commit `c1b3a4c`、`3450a28`、`002cd24`、`0e73551`、`bbde057`、`ea9eda6`）。

### 25.1 BGM（全应用背景音乐）
- **资源**：`res/raw/bgm.mp3`（`比武开始-古风`，已重制为**按节拍对齐的无缝循环**：BPM≈92、跳过前奏、42 小节、导出时在断点处做约 100ms 交叉淡变去除接缝）。
- **播放**：`BgmManager` 持有单一 `MediaPlayer`，`setLooping(true)` 直接循环；播放控制由 `GlobalApplication` 的 `ActivityLifecycleCallbacks` 驱动，**界面跳转不重启播放**，故听感无缝。
- **音量**：`setVolume(0.4f, 0.4f)`，低于短音效（`challenge_sound`/`dida`/`win`/`lose`/`send`），避免盖过落子/提示音。
- **控制逻辑（与代码一致）**：
  - 前台态统计：回调中对非 `AuthActivity` 的前台 Activity 计数；计数 >0 时 `setForeground(true)` 播放。
  - **登录/注册页不播放 BGM**：`onActivityStarted/Stopped` 中 `instanceof AuthActivity` 直接 return。
  - App 切后台（`activityCount<=0`）暂停，回前台恢复。
  - 音乐开关：`CowDungPrefs.musicEnabled`（默认 true），由声音设置弹窗控制；关闭即暂停。

### 25.2 声音设置弹窗（SoundSettingsDialog）
- **入口**：主菜单「声音」按钮（`btnSound`）→ `SoundSettingsDialog.show(...)`。
- **双独立开关**，均持久化到 `CowDungPrefs`：
  - **音效** `soundEnabled`：控制落子/提示等短音效（`SoundPool`），变化经 `onSfxChanged` 回调即时刷新。
  - **音乐** `musicEnabled`：控制 BGM，变化直接驱动 `BgmManager.setMusicEnabled`。
- **样式**：标题"声音设置"，两行开关（音效/音乐）间加分隔线，整体圆角背景（`dialog_round_bg`）。

### 25.3 登录/注册页视觉与密码眼睛
- `AuthActivity` 整页**背景图 + 中央小白花**（`white_flower`）装饰，白色圆角卡片叠于其上。
- 密码框右侧**眼睛图标**（`ic_eye_open` / `ic_eye_closed`）切换明文/密文；密文统一渲染为 `*`（个数=密码长度），由状态变量控制图标与显隐，不残留明文。

### 25.4 排行榜加载动画
- `RankingBoard` 拉取 `getRanking(1000)` 期间显示**加载中转圈**，数据返回后再渲染列表，避免空面板闪烁。

---

*本文档基于 `D:\flower_cowdung` 当前源码（`app/src/main/java/...`、`supabase/*.sql`、`res/raw/bgm.mp3`）整理，取代旧版 v2.1 中虚构的架构描述；v3.1 在 v3.0 基础上补齐 BGM、双开关声音、登录视觉与密码眼睛、排行榜加载动画等落地功能。*
