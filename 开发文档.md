# 《鲜花与牛粪》游戏 — 完整开发文档

> 版本：v2.1（重建版）
> 技术栈：客户端 **Android Studio（纯 Java / 代码动态布局）** + 后端 **Supabase（Auth / PostgreSQL / Realtime / RLS）**

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术选型](#2-技术选型)
- [3. 三种游戏模式总览](#3-三种游戏模式总览)
- [4. 核心游戏规则（基准规则）](#4-核心游戏规则基准规则)
- [5. 棋盘布局与交互](#5-棋盘布局与交互)
- [6. 胜负判定与电脑 AI 双用设计](#6-胜负判定与电脑-ai-双用设计)
- [7. 模式一：人机对战（单机）](#7-模式一人机对战单机)
- [8. 模式二：大厅游戏（随机路人对局）](#8-模式二大厅游戏随机路人对局)
- [9. 模式三：私密房间（好友约局）](#9-模式三私密房间好友约局)
- [10. 排名系统（积分制）](#10-排名系统积分制)
- [11. 用户身份体系](#11-用户身份体系)
- [12. 心跳 / 离线 / 断线恢复](#12-心跳--离线--断线恢复)
- [13. 聊天系统（可选增强）](#13-聊天系统可选增强)
- [14. Supabase 数据模型](#14-supabase-数据模型)
- [15. Supabase Realtime 频道与消息协议](#15-supabase-realtime-频道与消息协议)
- [16. 后端 RPC / 触发器设计](#16-后端-rpc--触发器设计)
- [17. Android 工程结构](#17-android-工程结构)
- [18. 核心页面与界面规范](#18-核心页面与界面规范)
- [19. 游戏状态机与状态同步](#19-游戏状态机与状态同步)
- [20. 关键流程时序](#20-关键流程时序)
- [21. 安全与权限（RLS）](#21-安全与权限rls)
- [22. 开发里程碑](#22-开发里程碑)
- [23. 验收标准](#23-验收标准)
- [24. 关键业务取舍清单](#24-关键业务取舍清单)
- [25. 大厅坐席状态机与退出规则（统一规则）](#25-大厅坐席状态机与退出规则统一规则)

---

## 1. 项目概述

- **项目名称**：鲜花与牛粪
- **游戏定位**：双人实时回合制棋类（Nim 取子变体），提供三种模式：**人机对战 / 大厅游戏 / 私密房间**。
- **一句话规则**：玩家轮流拾取鲜花，最终被迫拿到场上最后一枚"牛粪"的玩家判负。
- **积分制**：每局胜负写入数据库，按**最终得分**排名。大厅获胜 +1 分、私房获胜 +2 分（鼓励好友约局）、失败一律 −1 分；人机对战不计分。
- **基础形态**：卡片式棋盘（6 排），每排若干枚棋子（🌹 / 💩），点选花即可落子。

## 2. 技术选型

| 分层 | 选型 | 说明 |
| --- | --- | --- |
| 客户端 | Android Studio + **纯 Java（代码动态布局）** | 不用 XML 布局/Compose，全部 View 代码手工拼装 |
| 架构模式 | 轻量 MVC（Activity + 自定义 View） | Activity 持状态，自定义 View 封装棋子/桌子/地板 |
| 本地存储 | SharedPreferences | 存 user_id + 昵称 + 性别 + Supabase 会话 |
| 后端 | **Supabase** | 云托管：Auth / Postgres / Realtime |
| 后端访问 | Supabase **REST（HttpURLConnection）** | 轻量自研客户端，无第三方 SDK |
| 认证 | Supabase Auth（**匿名登录 Anonymous Sign-In**） | 全局唯一 user_id，无游客 |
| 敏感词过滤 | **`com.github.houbb:sensitive-word:0.29.5`** | 自带中英文词库（拼音/同音/中介字遮挡） |
| 数据库 | Supabase PostgreSQL + **RLS** | 用户 / 桌 / 房间 / 对局 / 排名 |
| 实时通信 | Supabase **Realtime（Presence + Broadcast）** | 对局落子、在线状态、聊天 |
| 服务端逻辑 | Supabase **Edge Functions / PG 定时任务** | 桌号/房号生成、空桌清理、排名聚合 |

> Supabase 迁移说明：旧版 MQTT（Paho）自建在线与广播 → 全部用 Supabase Realtime 替代，MQTT 代码整体删除。

> **实现约定（重要）**：客户端为纯 Java 动态布局。所有界面（启动页/大厅/私房/对局页）均用 `Activity` 内 `new View(...)` 代码构建，不引入 Compose 与 XML 布局文件。自定义 View 负责游戏视觉：`TableDishView`（桌子+四边头像）、`FloorView`（地板砖背景）。

## 3. 三种游戏模式总览

| 模式 | 入口 | 对手 | 是否需要网络 | 计分 |
| --- | --- | --- | --- | --- |
| **模式一：人机对战** | 首页「人机对战」 | 电脑 AI（Nim 最优策略） | 可选（离线可玩） | **不计分** |
| **模式二：大厅游戏** | 首页「大厅」 | 随机路人（匹配入桌） | 必须在线 | **计分**（胜 +1，败 −1） |
| **模式三：私密房间** | 首页输入 4 位房间号 | 指定好友 | 必须在线 | **计分**（胜 +2，败 −1） |

- 三种模式共用同一套核心规则引擎与棋盘组件。
- 大厅/私房对局结束后走统一结算流程：写 `games` 对局记录 → 更新双方**分数**与**胜/败场数** → 刷新排名。
- 人机对战仅复盘，不写入 `games`、不改变任何分数与场次。

## 4. 核心游戏规则（基准规则）

> 雷打不动的核心，任何页面改动不得违背。

### 4.1 基础规则
1. 对局双方轮流操作，每回合**从某一排拿走 1 枚或任意多枚鲜花**。
2. 一次只能从**一排**拿，不支持跨排拿取。
3. **牛粪不能被拿起或选中**，牛粪排固定保留 1 枚。
4. 鲜花（第 2~6 排）被全部拿完时，**取空最后一朵鲜花的玩家获胜**；轮到的一方被迫拿走牛粪即判负。
5. 不存在平局、不存在和棋。

### 4.2 先手规则
- **模式一（人机）**：**人（玩家）先手**。「开始游戏」首局玩家先手、第二局电脑先手（按局数轮换）。
- **模式二（大厅）**：**谁先入座谁先手**（先入座者即 A 座，开局定 `turnUserId=A`）。
- **模式三（私房）**：**谁先入座谁先手**（房主即先入座者，即 A 座）。
- **人人对战统一规则**：大厅与私房均为**谁先入座谁先手**；开局由 `mark_ready` 服务端原子判定，双方均点「准备好了」后才进入对局并确定先手。

### 4.3 中盘停摆
- 未开始等待期间，棋盘处于"待机展示"状态，只展示不可点。
- 对局结束后回到待机状态，等待下一位/下一局。

### 4.4 回合倒计时
- **轮到谁操作，该玩家面前显示倒计时**（`turnDeadlineAt = 当前时间 + 秒数`）。
- **真人对战（大厅/私房）**：每人 45 秒，超时判负。
- **人机对战（实际实现）**：玩家 180 秒、电脑 5 秒思考；倒计时归零处理按超时判负处理（人机当前以宽松时长为主，不构成主要压力）。
- 服务端在每回合切换时写入 `turnDeadlineAt`，所有端（双方 + 观战）以服务端时间为准同步显示。
- 倒计时归零仍未落子 → **该玩家超时判负**（对方获胜）。

## 5. 棋盘布局与交互

### 5.1 布局定义（核心，勿改）

| 排序号（索引） | 初始棋子数 | 内容 | 可否选择 |
| --- | --- | --- | --- |
| 第 1 排（i=0） | 1 | 💩 牛粪 | ❌ 永久禁止 |
| 第 2 排（i=1） | 2 | 🌹🌹 | ✅ |
| 第 3 排（i=2） | 3 | 🌹🌹🌹 | ✅ |
| 第 4 排（i=3） | 4 | 🌹🌹🌹🌹 | ✅ |
| 第 5 排（i=4） | 5 | 🌹🌹🌹🌹🌹 | ✅ |
| 第 6 排（i=5） | 6 | 🌹🌹🌹🌹🌹🌹 | ✅ |

- 数据结构固定：`remainingFlowers = int[]{1, 2, 3, 4, 5, 6}`（索引 0 为牛粪，恒为 1）。
- 选中结构：`selectedFlowers[行][朵] boolean 数组`，长度 = 该排棋子数。
- 选中态视觉：被选中的 🌹 透明度降为 50%（变暗），再点一次取消。

### 5.2 交互规则
- 点击 🌹 → 选中/取消。切换排时清空上一排选中（一次只操作一排）。
- 按钮实时显示"鲜花拿来 (N 朵)"。
- 点击「确认选择 / 鲜花拿来」→ 执行 `remainingFlowers[selectedRow] -= selectedCount`。
- 点击 💩：提示"不能拿牛粪，只能拿鲜花！"，不产生状态变化。

### 5.3 棋盘 UI 布局（实际）
自上而下：对战信息条 8% / 棋盘区 42% / 操作按钮区 10% / 日志+聊天区 40%。纯 Java 动态布局，用 `LinearLayout`（vertical）+ `weight` 表达占比。
- **双人并排**：大厅/私房对局时，**双方并排坐在棋盘下方**，双方头像并排显示在棋盘下方两侧（头顶各自昵称），各自面前一个独立倒计时区域。
- **倒计时**：轮到谁，谁面前的倒计时激活并倒数（真人对战 45s，人机玩家 180s/电脑 5s，见 4.4）；另一方显示等待状态。
- **提示走法**：长按「确认选择」3 秒 → 高亮推荐拿走的花（调用 `ComputerAI`，见 6 章）。

## 6. 胜负判定与电脑 AI 双用设计

### 6.1 胜负判定流程（统一，客户端为主、服务端复核）
- 计分对局（大厅/私房）：
```
每回合落子后：
  sum = 第2~6排剩余数量总和
  if sum == 0:
      拿空最后一朵花的玩家为 winner（对手被迫拿牛粪）
      调 RPC finish_game(winner, loser, room_type)
      → 写 games + 按模式计分（大厅胜+1 / 私房胜+2 / 败方-1）+ 更新双方 wins/losses
      广播结果 → 排行榜刷新 → 重置棋盘
```
- 人机对战：同样判定胜负，但**只显示结果、不调 finish_game、不计分**。

### 6.2 电脑 AI 模块（ComputerAI）—— 一个模块两种用法

**为什么抽成独立模块：**
- **用法一 / 人机对战**：轮到电脑时直接调用，拿最优走法落子。
- **用法二 / 双人提示**：双人对局（大厅/私房）时后台常驻一个"机器人管家"盯盘，开发者可**在特定时机调用同一模块**给任意一方提示"下一步该怎么走"，用于测试与教学。

**算法（Nim 最优策略）：**
- `nimSum = r1 ^ r2 ^ r3 ^ r4 ^ r5`。
- `nimSum != 0`：必胜局面 → 找使 nimSum 归零的走法（`take = r[i] - (r[i] ^ nimSum)`）。
- `nimSum == 0`：必败局面 → 非空排中随机拿。
- 无棋可走 → 返回 null（对局已结束）。

**模块接口（静态，随时可调）：**
```
ComputerAI.getNextMove(int[] remainingFlowers) : Move {row, count} | null   // 落子 / 提示共用
ComputerAI.getHint(int[] remainingFlowers)     : Move | null               // 明确语义的提示入口
```

**双人模式后台机器人设计：**
- 后端/房间状态机在每次落子后维护棋盘快照；开发者侧可随时调用 `ComputerAI.getHint(state)`。
- 开发者打开"提示开关 / 长按提示按钮" → 在棋盘上高亮推荐拿走的花。
- 该机器人**只读、不落子**，不影响正式对局流程，属于测试/辅助能力。

## 7. 模式一：人机对战（单机）

### 7.1 流程
```
首页 → 人机对战 → 开始游戏（先手按局数交替） → 玩家选花 → 电脑 AI 落子 →
判胜 → 本地显示结果 → 再来一局
```

### 7.2 特性
- 断网可玩；**不计分、不影响排名与场次数据**，仅本地复盘。
- 电脑 AI 使用 Nim 最优策略；必败局面随机走。
- 可开启"提示"：任意时刻长按按钮，机器人提示下一步最优走法。

### 7.3 结算
- 只在本机显示"您赢了 / 电脑赢了"结果文案与日志。
- **不调用 finish_game、不写 games、不改变分数/胜败场次/排名**。

## 8. 模式二：大厅游戏（随机路人对局）

### 8.1 桌子标识与生命周期
- 随机**短字符串桌号**（如 `aK3x9q`），非递增数字号。
- 压力上升自动建桌；所有人离开且超时 → 删除空桌。
- 全局桌子**最大硬上限**（如 200 张）。

### 8.2 入座规则（实际：点桌选择 + 玩家门禁）
- 每桌固定 **2 个对战座位**，其余人只能观战。
- **点桌弹出选择框【坐下玩游戏 / 当观众 / 取消】**：
  - 坐下玩游戏：先校验玩家身份（昵称+性别建档，见第 11 章），未完善则弹窗引导设置，**设置完成才能开局**。
  - 当观众：以观众身份进入对局页（`asWatcher`），进入时对局页日志区提示「XXX来了!」。
- 先入座者先手；第二位入座即可开局。
- 大厅观战只展示"观战人数"数字（后续 13 章聊天：大厅不展示观战名单）。

### 8.3 结算与排名
- 对局结束 → `finish_game` RPC → 按大厅规则更新双方 score（胜 +1 / 败 −1）与 wins/losses → 大厅排行榜实时变化。

## 9. 模式三：私密房间（好友约局）

### 9.1 房间标识与准入
- **4 位随机数字房间号**（0000~9999），仅凭房号进入，不设密码。
- 10000 种组合暴力猜号概率低；不在大厅列表展示。
- **玩家身份进入**：点房内桌子选「坐下玩游戏」时校验昵称+性别建档（同大厅，见 11 章）；纯观战则无需玩家身份。

### 9.2 房主权限
- 创建房间者标记为**房主**，仅在线期间有效。
- 权限：**踢出观战者、禁言观战者**（保留观战权）。
- **禁止踢正在对局的 2 名棋手**。
- 房主退出/离线 → 房主标记永久清除，不转移；房间后续无管理员。

### 9.3 观战展示（实际）
- **对局页「观众列表」区展示完整观战者昵称列表**（大厅与私房均适用）。
- 有人进入观战 → 对局页日志区提示「XXX来了!」并加入列表。
- 下棋玩家**长按观众昵称** → 弹窗「确定将 XXX 踢出该房间吗?」→ 确认后移除名单并提示。联网时调用 `host_kick` RPC（仅房主可踢，且禁止踢棋手）。
- 私房展示完整观战者昵称列表（区别于大厅排行榜只显人数，见 13 章）。

### 9.4 结算与排名
- 对局结束 → `finish_game` RPC → 按私房规则更新双方 score（胜 +2 / 败 −1）与 wins/losses → 排行榜刷新。

## 10. 排名系统（积分制）

### 10.1 核心指标
- **规则**：每个用户累计**最终得分 `score`**（总得分 = 获胜得分 − 失败扣分）作为唯一排名依据。
- 计分标准：
  - **大厅游戏获胜**：+1 分
  - **私密房间获胜**：+2 分（鼓励好友约局）
  - **失败**：一律 −1 分
  - **人机对战**：不计分、不改变场次
- 展示字段：`score`、`wins`（胜场）、`losses`（败场）、`total_games`（总场次）。

### 10.2 入库时机
- 每局网游对局结束（大厅/私房）→ `finish_game` RPC 事务内原子完成：
  1. 插入 `games` 记录（room_type、参与者、winner、loser、moves、时间、score_delta）。
  2. 更新双方：`wins/losses += 1`、`total_games += 1`、`score += score_delta`。
- score_delta 规则：胜者按模式 +1 / +2，败者 −1（下表）。

| 模式 | 胜者得分 | 败者得分 |
| --- | --- | --- |
| 大厅游戏 | +1 | −1 |
| 私密房间 | +2 | −1 |
| 人机对战 | 不计 | 不计 |

- **防重入**：`games` 表加唯一约束 `(table_id, room_code, finished_at)` 或对局事务锁，保证一局只结算一次。
- **人机对局**调用本地判胜逻辑，**不写库**。

### 10.3 展示位置
- 首页顶部分区：**全局排行榜 Top 50**（头像 + 昵称 + 得分）。
- 对局/观战页显示对局双方头像与昵称、以及双方得分。
- **点击任意玩家头像** → 弹出该玩家资料卡：性别头像、昵称、**胜场数 / 败场数** / 总局数 / 当前得分。

### 10.4 排行榜实现
- 方式 A：直接 SQL 查询 `profiles` order by score desc（数据量小时优先）。
- 方式 B：Edge Function / PG 物化视图聚合（量大时），供 `get_ranking()` RPC 返回。
- 客户端定时（如 30s）或对局结束时刷新。

## 11. 用户身份体系（实际：匿名 + 玩家身份门禁）

### 11.1 首次启动
1. **静默匿名登录**：启动即调用 Supabase Auth 匿名登录（`POST /auth/v1/signup`，body `{}`），拿到全局唯一 `user_id` 与 access token，持久化本地（`CowDungPrefs`），后续启动自动恢复，无需重复登录。
2. **建档弹窗（主动引导，不强制）**：尚未在服务端 `profiles` 建档时，弹窗引导填写**昵称 + 性别**：
   - 昵称：非空；性别二选一【男/女】（决定本人头像 man/women）。
   - 提交调 `create_profile` RPC：重名自动追加后缀（名称2、名称3…），返回后回查服务端昵称以同步本地。
   - **外部校验**：登录失败/建档失败仅提示，不阻断界面（人机对战离线可玩）。

### 11.2 玩家身份门禁（核心规则）
- **匿名 = 不是玩家**。只有「已设置昵称 + 性别并在服务端建档」的用户才具备**玩家身份**。
- **游戏大厅 / 私密房间点「坐下玩游戏」前**，先校验 `hasPlayerIdentity()`：
  - 已完善 → 直接入座开局。
  - 未完善 → 弹窗提示「匿名只能当观众或玩人机对战，设置昵称和性别后才能当玩家」，点「去设置」进入建档弹窗；**不设置则不能当玩家**。
- **匿名用户可做的事**：人机对战（离线）、作为观众观看他人对局。
- **启动提示**：主菜单启动时若为匿名态，界面给出提示文案，明确「匿名不可当玩家，只能和机器人玩」。

### 11.3 后续启动
- 读取本地 user_id + 昵称 + 性别，用持久化的 access/refresh token 恢复登录（过期自动刷新；刷新失败则重新匿名登录）。

### 11.4 核心原则
- 一切权限与状态判定只依赖 `user_id`；昵称与性别仅展示与决定头像。
- 匿名登录保证无"无身份用户"，但**玩家入场权限**由昵称/性别建档决定。

## 12. 心跳 / 离线 / 断线恢复

- **前台**：周期发送心跳（Realtime Presence 在线标记）。
- **切后台/锁屏**：利用系统窗口期发一条"遗言"上报状态（不依赖送达）。
- **离线判定**：以心跳超时为准。占用座位离线 → 释放座位；观战者 → 移出。
- **断线重连**：重登 Auth → 拉取 `lobby_tables.game_state` / `rooms.game_state` 恢复棋盘。

## 13. 聊天系统 + 敏感词过滤（已实现）

- 只纯文字，砍语音/图片/表情；不落库，Realtime 广播，无历史。
- 限流：间隔 ≥ 1 秒，单条 ≤ 200 字符。
- 私房房主可禁言观战者；大厅无管理员。
- **敏感词过滤（已实现）**：
  - 依赖 `com.github.houbb:sensitive-word:0.29.5`（与 shrimpfarm 项目同款），自带中英文大词库，支持拼音/同音字/部首拆分/中介字遮挡。
  - 封装类 `BadWordFilter`：`containsBadWord(text)` 判断、`filter(text)` 将命中词替换为 `*`。
  - 所有聊天发送统一经 `BadWordFilter` 过滤后再广播；命中时额外提示「检测到不文明用语，已自动屏蔽」。
  - 观战提示、昵称、对局日志不参与聊天过滤（仅用户聊天内容过滤）。

## 14. Supabase 数据模型

> **已落地**：对应建库脚本在 `supabase/schema.sql`，已在 Supabase SQL Editor 分三段执行成功。
> 全部表启用 RLS（详见 21 章）。聊天消息不落库。

### 14.1 profiles（用户资料 + 排名统计）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid PK, refs auth.users | 全局唯一 user_id |
| nickname | text UNIQUE | 昵称（服务端保证重名校验） |
| gender | text | `male` / `female`（决定头像样式） |
| score | int default 0 | 最终得分（排名唯一依据） |
| wins | int default 0 | 获胜场次 |
| losses | int default 0 | 落败场次 |
| total_games | int default 0 | 总局数（网游对局） |
| last_seen_at | timestamptz | 最近心跳时间 |
| created_at | timestamptz | 注册时间 |

> 排名即按 `score DESC, created_at ASC` 排序。人机对战不计分、不计场次。

### 14.2 lobby_tables（公开大厅桌子）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | text PK | 随机短字符串桌号 |
| status | text | `waiting` / `playing` / `finished` |
| player_a_id | uuid nullable | 座位 A（先手） |
| player_b_id | uuid nullable | 座位 B |
| current_turn_id | uuid nullable | 当前应落子玩家 |
| game_state | jsonb | 棋盘序列化（见 19 章） |
| watcher_count | int default 0 | 观战人数（仅数字） |
| last_active_at | timestamptz | 最近活跃（闲置清理） |
| created_at | timestamptz | 创建时间 |

### 14.3 rooms（私人房间）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| room_code | char(4) PK | 4 位数字房间号 |
| host_id | uuid | 房主（瞬时权限，不持久） |
| status | text | `waiting` / `playing` / `finished` |
| player_a_id | uuid nullable | 座位 A（先手，通常房主） |
| player_b_id | uuid nullable | 座位 B |
| current_turn_id | uuid nullable | 当前应落子玩家 |
| game_state | jsonb | 棋盘序列化 |
| last_active_at | timestamptz | 最近活跃 |
| created_at | timestamptz | 创建时间 |

### 14.4 room_members（私人房间在线成员/观战名单）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| room_code | text, refs rooms | 房间号 |
| user_id | uuid | 成员 |
| role | text | `player` / `watcher` |
| is_muted | bool default false | 房主禁言标记 |
| joined_at | timestamptz | 加入时间 |

> 大厅观战只显示人数（watcher_count）；私房用 room_members 展示全名单。

### 14.5 games（对局记录 / 排名数据来源）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid PK | 对局 ID |
| room_type | text | `lobby` / `private`（人机不计分不写库） |
| table_id | text nullable | 关联大厅桌 |
| room_code | char(4) nullable | 关联私房 |
| player_a_id | uuid | 先手方 |
| player_b_id | uuid | 后手方 |
| winner_id | uuid | 获胜方 |
| loser_id | uuid | 落败方 |
| score_delta | jsonb | `{winner: +1|+2, loser: -1}` 计分增量 |
| moves | jsonb | 落子记录（可选） |
| finished_at | timestamptz | 结束时间 |

> 排名由 `finish_game` RPC 在事务内原子更新 `games` + `profiles.score/wins/losses/total_games`。

### 14.6 索引建议
- `profiles(score DESC)`：排行榜。
- `games(room_type, finished_at DESC)`：对局历史。
- `games(table_id)` / `games(room_code)`：对局查询。

## 15. Supabase Realtime 频道与消息协议

### 15.1 Presence（在线状态 / 心跳）
- 频道：`online`。
- `track({ user_id, nickname, scope })`（scope = 所在桌/房）。
- `onJoin/onLeave` 维护在线；离线以心跳超时为准。

### 15.2 Broadcast（实时业务）
| 频道 | 事件 | payload 说明 |
| --- | --- | --- |
| `table:{tableId}` | `game_state` | 棋盘状态 JSON（见 19 章） |
| `table:{tableId}` | `move` | `{player_id, row, count}` |
| `table:{tableId}` | `chat` | `{sender_id, nickname, text}` |
| `table:{tableId}` | `join/leave` | 入座/离座/观战人数变更 |
| `room:{roomCode}` | `game_state` | 棋盘状态 JSON |
| `room:{roomCode}` | `move` | 同大厅 |
| `room:{roomCode}` | `chat` | 同大厅 |
| `room:{roomCode}` | `mute/kick` | 房主操作观战者 |
| `room:{roomCode}` | `join/leave` | 入座/离座/观战名单变更 |

> 消息只广播不落库，符合"聊天无历史"要求。

### 15.3 落子同步要点（通知仅作信号，必须拉取为准）
- **原则**：Realtime 通知**不是棋子移动的结果数据**，只作为"有变化"的触发信号。客户端收到通知后**主动向服务端拉取数据，以拉取到的服务端数据作为移动/渲染棋子的唯一基准**。
- 落子方：本地乐观操作 → 提交 `update_game_state` RPC 写 DB → 收到 `game_state` 通知 → **发起一次 DB 拉取** → 按返回结果校准棋盘。
- 对方/观战：收到 `game_state`/`move` 通知 → 同样**主动拉取 DB `game_state`** → 按返回渲染，**不直接信任通知 payload 里的棋盘字段**。
- 拉取接口：`get_game_state(table_id|room_code)` RPC（或直接读表行）。
- 连发/乱序：多次触发只拉最新一次；本地以 DB 返回 + `turnDeadlineAt` 校准倒计时。
- 断线重连：进入桌/房后先主动拉取一次 `game_state` 恢复，再开启监听。

## 16. 后端 RPC / 触发器设计

> **已落地**：下方所有 RPC / 触发器已在 `supabase/schema.sql` 实现并在 Supabase 执行。

| 名称 | 类型 | 职责 |
| --- | --- | --- |
| `check_nickname(n)` | RPC | 昵称重名校验 |
| `create_profile(nick, gender)` | RPC | 创建资料（性别头像 + 处理重名自动加后缀） |
| `create_lobby_table()` | RPC | 生成随机短桌号建桌（超上限报错） |
| `join_table(id) / leave_table(id)` | RPC | 原子入座/离座、座位释放 |
| `create_room() / join_room(code) / leave_room(code)` | RPC | 私人房间生命周期 |
| `update_game_state(...)` | RPC | 原子更新棋盘 + 回合切换（写入 turnDeadlineAt）+ 扣时判定 |
| `get_game_state(table_id|room_code)` | RPC | 拉取当前权威棋盘状态（通知后校准基准） |
| `finish_game(...)` | RPC | **结算：写 games + 原子更新双方 wins/losses/total_games** |
| `get_ranking(limit)` | RPC | 返回排行榜 Top N |
| `host_kick(room, user)` / `host_mute(room, user)` | RPC | 房主权限（校验房主身份） |
| 定时清理 | PG cron / Edge | 删除超时空闲桌、空房、离线座位 |

> 并发一致性全部在 RPC/触发器内用 SQL `FOR UPDATE` 或原子更新保证，客户端不做服务端判定。

## 17. Android 工程结构（实际：纯 Java 动态布局）

```
app/src/main/java/com/example/cowdunggame/
├── GlobalApplication.java       // Application，初始化崩溃上报
├── MenuActivity.java            // 启动页 = 实时游戏大厅（30 桌滚动，平板 3 列/手机 2 列）
├── MainActivity.java            // 对局页（棋盘 + 操作栏 + 双人栏 + 观众列表/日志 + 聊天）
├── PrivateRoomActivity.java     // 私人房间（创建/加入 → 单桌大厅，房号显示在桌面）
├── TableDishView.java           // 一桌组件：桌面 2.5×头像、头像↔桌面 8dp，四边预留位(玩家/观众)
├── FloorView.java               // 地板砖背景（浅蓝）
├── ComputerAI.java              // Nim 最优策略（唯一算法核心）—— 双用模块
├── BadWordFilter.java           // 敏感词过滤（封装 houbb sensitive-word）
├── SupabaseClient.java          // 轻量 Supabase REST 客户端（HttpURLConnection，匿名登录/RPC/会话持久化/心跳）
├── SupabaseIdentity.java        // M1 身份闭环：匿名登录 + 昵称/性别建档 + 玩家身份校验
├── RealtimeClient.java          // Supabase Realtime 订阅（OkHttp WebSocket，Phoenix channel）
├── UpdateManager.java           // 版本更新检测
├── CrashHandler.java            // 全局崩溃捕获
└── CrashReportActivity.java     // 崩溃详情页
```

> 核心数据模型无独立 model 类：棋盘状态用 `int[] remainingFlowers` + `boolean[][] selectedFlowers`；玩家身份/会话存 SharedPreferences（`CowDungPrefs`）；服务端 `game_state` 用 JSON（`org.json`）。

### 17.1 依赖（app/build.gradle 实际）
```
implementation 'androidx.appcompat:appcompat:1.6.1'
implementation 'com.google.android.material:material:1.12.0'
implementation 'com.github.houbb:sensitive-word:0.29.5'   // 敏感词过滤
implementation 'com.squareup.okhttp3:okhttp:5.4.0'        // Realtime WebSocket
implementation 'com.google.code.gson:gson:2.14.0'         // Realtime Phoenix 消息解析
```
> 后端访问不用 Supabase 官方 SDK，使用自研 `SupabaseClient`（HttpURLConnection + REST）+ `RealtimeClient`（OkHttp WebSocket）。旧版 MQTT 依赖（`org.eclipse.paho:*`）与 `MqttForegroundService` 已删除。

## 18. 核心页面与界面规范（实际页面）

### 18.1 启动页 = 实时游戏大厅（MenuActivity）
- 背景浅蓝地板砖；启动即判断**平板(每行 3 桌)/手机(每行 2 桌)**，固定渲染 **30 桌**滚动列表（真实数据不足用空桌 idle 图补齐，四边半透明预留位始终可见）。
- 桌面边长 = 头像边长 × 2.5；头像↔桌面固定 8dp；人与人、人与屏幕边距 = 头像一半；行距 12dp（`TableDishView.computeRowLayout` 精确计算）。
- 底部透明悬浮按钮：【人机对战】【私人房间】【新建桌子】；点任意已建桌弹出【坐下玩游戏 / 当观众】。
- 启动后台 `SupabaseIdentity.ensureIdentity()`（匿名登录 + 首次建档弹窗），随后 `RealtimeClient` 订阅实时刷新 + 60s 心跳 + 30s 兜底刷新。

### 18.2 游戏大厅 —— 已合并进启动页（MenuActivity），无独立页面

### 18.3 私人房间（PrivateRoomActivity）
- 进入弹【创建房间 / 加入房间】（加入需输 4 位房号）；成功即进入**单桌大厅**，布局逻辑与大厅一致，只是仅 1 张居中大桌子，**房间号显示在桌面上**。
- 点桌子弹【坐下玩游戏 / 当观众】：坐下需玩家身份校验；当观众调 `join_room` 入房 watcher。
- 数据走 Supabase `create_room/join_room/leave_room` + Realtime 订阅 rooms/room_members/profiles 实时刷新；顶栏「复制房号」。

### 18.4 对局页（MainActivity）
自上而下：
1. 棋盘区（6 排，见 5.3），高度 = 屏高 42%。
2. 操作按钮区（金色外框，5 格）：【我的昵称】【我的倒计时/退出棋局】【确认选择/鲜花拿来】【电脑倒计时】【电脑昵称】。提示走法=长按「确认选择」3 秒。
3. 日志区：
   - 标题 **「观众列表」**：列出进入观战的人（`XXX来了!` 提示 + 名单）。
   - 玩家**长按观众昵称 → 弹窗踢出该观众**（联网时调 `host_kick` RPC）。
   - 「游戏日志:」副标题下显示本局落子记录与胜负结算。
4. 底部聊天栏：昵称按钮(15%) + 输入框(70%) + 发送(15%)，发送前经 `BadWordFilter` 过滤敏感词。

### 18.5 排行榜
- 未单独页面；`get_ranking` RPC 已就绪，后续按 18.3 原规划接入首页/专属页。

## 19. 游戏状态机与状态同步

### 19.1 状态机
```
IDLE ──开始──▶ PLAYING（一方落子）──▶ 判胜/判负 ──▶ FINISHED（结算入库）──重置──▶ IDLE
   ▲                                                     │
   └────────────────── 断线恢复（拉取 game_state）───────────┘
```

### 19.2 game_state JSON 结构（固定协议）
```json
{
  "rows": [1, 2, 3, 4, 5, 6],    // 剩余棋子数，index 0=牛粪恒1
  "turnUserId": "uuid",          // 当前应落子者
  "turnDeadlineAt": "ISO8601",   // 本回合 45 秒倒计时截止（服务端权威）
  "totalFlowers": 20,
  "phase": "playing" | "finished",
  "winnerId": "uuid|null",
  "loserId": "uuid|null"
}
```
- 断线重连：进入桌/房后主动拉取 `game_state` 恢复。

### 19.3 同步方向
- 服务端 DB 是**唯一权威**；客户端收到任何 Realtime 通知后**主动拉取 DB 数据**再渲染，不直接采用通知内容落子。
- 落子方：乐观更新本地 → 提交 RPC → 收到通知 → 拉取 DB 覆盖校准。
- 对方/观战：仅监听通知 → 拉取 DB 更新。

## 20. 关键流程时序

### 20.1 首次启动（实际）
```
App 启动 → 匿名登录(Supabase Auth) → 有本地 user_id?
  ├─ 是 → 拉取本人 profiles 行 → 已建档?  → 是 → 静默进首页
  │                                 └─ 否 → 弹昵称+性别建档框(可跳过) → 调 create_profile → 首页
  └─ 否 → 匿名登录成功取得 user_id → 弹昵称+性别建档框(可跳过) → 首页
```
> 建档非强制：未建档 = 匿名态，可玩人机/观战，但点「坐下玩游戏」时会被门禁拦截引导建档。

### 20.2 人机对战
```
首页 → 人机对战 → 开始游戏 → 玩家/电脑轮流 → 判胜 → 仅本地显示结果（不计分、不调用 finish_game） → 再来一局
```

### 20.3 大厅路人入座
```
A 新建桌(随机桌号) → B 从列表入座 → 开局 → 轮流落子(Broadcast) → finish_game → 排名刷新
```

### 20.4 私房约局
```
房主创建房间得4位号 → 好友输入房号进入 → 开局 → 房主可踢/禁言观战 → finish_game → 排名刷新
```

### 20.5 断线恢复
```
切后台 → 遗言上报 → 心跳超时服务端释放座位 → 回前台 → 重连 Auth →
  若棋局未结束且未过重连窗口 → 拉取 game_state 恢复棋盘
  否则 → 释放座位回 IDLE（对局按防重入规则处理）
```

## 21. 安全与权限（RLS）

> **已落地**：全部策略已在 `supabase/schema.sql` 第 17 节实现并在 Supabase 执行。
> 认证头采用新版 publishable key（`sb_publishable_*`）+ 用户 JWT，配 RLS 后可安全公开给客户端。

- **profiles**：`select` 登录用户可见（排行需要）；`update`/`insert` 仅自己。
- **lobby_tables / rooms**：`select`/`insert` 登录用户开放；落子/结算走 RPC 校验（座位持有者才能动棋）。
- **room_members**：`select` 房间内成员可见；`insert` 自己；`delete` 仅自己或房主。
- **games**：`select`/`insert` 开放（排行榜/对局记录），更新走 RPC。
- 房主操作、落子校验均用服务端 RPC `auth.uid()` 二次校验，客户端不可伪造。

## 22. 开发里程碑（★=已完成）

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| M0★ | 基础工程 + 三大页面（启动页/大厅/私房）+ 图片桌组件 + 浅蓝地板 | 单机 UI 骨架 |
| M0★ | Supabase 建库脚本（表/RLS/RPC/cron）落地 `supabase/schema.sql` | 后端就绪 |
| M1★ | Supabase 匿名登录 + 玩家身份门禁（昵称/性别建档，匿名只能观战/人机） | 身份闭环 |
| M1★ | 敏感词过滤（houbb sensitive-word + BadWordFilter） | 文明发言 |
| M1★ | 观战进出提醒「XXX来了!」+ 观众列表 + 长按踢人 UI | 观战交互 UI |
| M2★ | 核心规则引擎 + ComputerAI 双用模块 + 人机对战（离线可玩） | 模式一可玩 |
| M3 | 大厅游戏：建桌/入座/回合广播/观战人数（对接 Realtime） | 模式二闭环 |
| M4 | 私密房间：房号/房主踢人禁言/观战名单（对接 host_kick/join_room） | 模式三闭环 |
| M5 | finish_game 结算 + 双方积分/胜败场入库 + 排行榜页面 | 排名闭环 |
| M6 | 回合计时(45s) + 心跳/离线/断线恢复 + 空桌清理 + 聊天广播 | 稳定运行 |
| M7 | UI 打磨、压测、发布准备 | 上线 |

## 23. 验收标准

1. 首次启动匿名登录；引导设置**昵称 + 性别**（可跳过=匿名态）；重名自动加后缀；头像区分男女、点头像可看胜败场。
2. **匿名用户不能当玩家**：点「坐下玩游戏」被门禁拦截并引导建档；匿名可玩人机或观战。
3. 三种模式入口齐全，共用同一核心规则。
4. 人机对战符合第 4 章规则；电脑 AI 满足 Nim 最优策略（必败局随机）；**人机不计分不写库**。
5. 大厅桌随机短 ID；超过硬上限无法新建；大厅观战只显人数。
6. 私房 4 位房号唯一准入；房主可踢/禁言观战，不能踢 2 名棋手；房主离线权限永久清除。
7. **每局积分正确入库**：计分对局统一走 finish_game，按 `score_delta`（大厅胜+1/私房胜+2/败-1）原子更新 score/wins/losses/total_games。
8. **排名正确**：按 score 降序，可展示**得分 + 胜场/败场/总场次**；本人名次高亮。
9. **回合计时**：轮到谁面前 45 秒倒计时（服务端 turnDeadlineAt 权威），超时判负。
10. 双人模式后台可随时调用 ComputerAI 提示下一步（自动/手动开关），只读不影响对局。
11. 心跳超时判离线：座位释放、观战移除；断线可恢复未完成棋局。
12. **同步以拉取为准**：客户端收到任何 Realtime 通知仅作信号，主动拉取 DB 数据再渲染。
13. 手机切后台/锁屏不直接判负；身份本地持久化、重启免重复登录。
14. **敏感词过滤生效**：聊天发送含脏话自动替换为 `*` 并提示。

## 24. 关键业务取舍清单

- 三种模式：人机对战（离线可玩、**不计分**）/ 大厅游戏（路人，胜+1）/ 私密房间（好友，胜+2）。
- 排名核心指标 = **最终得分 score**（获胜得分 − 失败扣分），辅助展示胜/败场数。
- 失败一律 −1；回合计时 45 秒超时判负（服务端权威）。
- 大厅不用数字房间号；私房 4 位房号即准入凭证、不设密码。
- 房主掉线/退出后权限永久回收，不支持转移/恢复。
- **匿名登录保证无"无身份"用户；但匿名 ≠ 玩家**——匿名只能人机/观战，需设置昵称+性别建档后才能当玩家（决定头像）。
- 大厅观战只显人数，私房展示全部观战名单；对局页有观众列表，玩家可长按踢出观战者。
- 聊天只文字、不持久化、无历史；**发送前统一过敏感词过滤（houbb sensitive-word）**。
- **客户端不信任本地与通知 payload，收到 Realtime 通知仅作信号，主动拉取服务端 game_state 为移动棋子的唯一基准。**
- **ComputerAI 单模块双用**：人机落子 + 双人后台提示，只读不干扰对局。

---

## 25. 大厅坐席状态机与退出规则（统一规则）

> 本节归纳产品要求的**大厅 / 坐席 / 退出 / 异常退场**完整规则。
> 该规则对**人机大厅、人人大厅、私密房间**三处一视同仁——三者本质是同一套逻辑，
> 差异仅在于"对手是谁、桌子数量、是否有房间号准入"。

### 25.1 三种模式的本质关系

| 模式 | 与"统一规则"的关系 | 唯一差异 |
| --- | --- | --- |
| **人机大厅（PvE）** | 完全一致的坐席/观战/退出逻辑 | 对手是电脑 AI；多张预置桌 |
| **人人大厅（PvP）** | 完全一致的坐席/观战/退出逻辑 | 对手是另一个真人玩家；多张桌 |
| **私密房间** | 同人人大厅 | **只有一张桌子 + 一个 4 位房间号**，仅凭房号准入、不在大厅列表公开 |

一句话：**私密房间 = 一张带房号的人人对局桌**；人人 / 人机大厅 = 多张同款桌并排展示。

### 25.2 身份与两种站位（站立 / 坐下）

- 每个进入大厅 / 房间的用户都**带着身份**（全局唯一 user_id + 昵称/性别头像）。
- **站立**：在走道/大厅中能看到**全部桌子的完整状态**——谁在玩、谁在观战、观众数。
- **坐下**：看中某桌后坐下进入棋局画面，**只能看到/操作当前这一局的棋盘**。

### 25.3 入座规则

| 目标桌状态 | 可做的动作 |
| --- | --- |
| 桌上**没有玩家**（空闲） | 「坐下当玩家」或「坐下当观众」二选一 |
| 桌上**已有玩家**（一人/对局中） | 只能「坐下当观众」 |

- **观战不限量**：不管多少观众都能坐下，观众位置不设上限。
- 无论当玩家还是当观众，**都必须"坐下"**（桌旁有凳子/椅子），不存在"站着看棋"。
- 从大厅看某桌 -> 坐下（玩家或观众）-> 进入该桌棋局。

### 25.4 退出规则

- **「退出」的含义**：站起离座，状态回到"刚进入大厅"的样子——**带身份站在大厅，能看全所有桌子**，并可再次坐下。
- **观众退出**：按「退出观战」按钮或系统返回键 → 直接返回大厅站立状态（无二次确认弹窗需求）。
- **玩家退出**，分两种情况：
  1. **棋局已结束（停摆/待机状态）**：可直接按「退出棋局」按钮离座。
  2. **棋局进行中（playing）**：按「退出棋局」/返回键 → **弹窗确认**，确认离场后**系统判定该玩家为输**，再回大厅站立。
- 玩家/观众一旦确定退出，**座位/观战关系必须立刻写入数据库**，让大厅所有站着的人立即看到该座已空——**不允许"退出去后还占着座、还得再点桌子点退出座位"这种多余步骤**。

### 25.5 异常退场：遗言机制

- 用户直接**划掉/清除应用进程**（不点任何退出按钮）时，客户端无法走正常退出流程。
- 必须用**"遗言"**方式把状态落地：
  1. **进程存活期间持续上报心跳**（刷新 `last_active_at`）。
  2. 应用被系统回收/划掉前，尽力发送一条"遗言"：离座（玩家释放座位；观众移除观战关系；对局中则判负）。
  3. **服务端超时兜底**：心跳超时（如 15 分钟无心跳）由定时任务自动释放座位、移除观战关系。

### 25.6 状态流转总图（玩家视角）

```
站立（大厅，可见全部桌子）
  │ 选桌坐下
  ├──► 坐下当玩家 ──► 棋局画面（只看本局）
  │        ├─ 局结束(停摆) ──按「退出棋局」──────┐
  │        └─ 对局进行中 ──按退出口/返回 → 弹窗确认 → 判负 │
  └──► 坐下当观众 ──► 棋局画面（只读本局）
            └─ 按「退出观战」/返回 ────────────────┘
                        │
                        ▼
        退出（离座写库）→ 站立（大厅，可见全部桌子）→ 可再次坐下
```

### 25.7 对当前实现的修正要求（截至重写版评估）

1. **退出即离座写库**：坐席/观战关系在确认退出的瞬间写入数据库，大厅刷新后立即显示空座。
2. **玩家对局进行中退出 = 判负**：先判定玩家输（写胜负/结算），再释放座位，再回大厅。
3. **无需"点击桌子再退出座位"的二次操作**：退出按钮直接离座回大厅。
4. **遗言清座**：进程被杀也要把状态写库（心跳 + 遗言 + 服务端超时兜底）。